#include<iostream>
#incldue<stack>
#include<string>
using namespace std;
int main(){
		//Algorithm for prefix to postfix
		string s;
		cin>>s;
		stack<char> st;
		int len=strlen(s);
		for(i=len;i>=0;i--){
			char c=s[i];
			if()
		}
}
